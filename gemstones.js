function gemstones(arr) {
    // Write your code here
    //I am looking for gemstones in the array
    //to find the gemstones I need to look at each string in the array and see
    //if the element repeats itself in each element of the array
    //if it does then it is a gemstone and I need to count it.
    
    //I need to create a variable to store the gemstones.
    let gemstones = [];
    //I need to loop through the array 
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
        console.log(arr[i]

        )
    };
//    for (let index = 0; index < arr.length; index++) {
//     const rocks = arr[index];
//     console.log(rocks)
//     console.log(rocks[1])
    
//    }
   
    
    //loop through each rock in the first string and store this to a variable
    
}
    // then I need a way to count how many times the rock repeats itself in the other strings
    //then I need an if statement that will check if the rock repeats itself in all the other strings in the array
    // if it does repeat itself in all the other strings in the array I will add the rock to the gemstones variable
    // if the rock does not repeat itself in all the other strings in the array I will not add it    


console.log(gemstones(["abcdde", "baccd", "eeabg"]));